package com.vinay.sample.service;

public class HelperService {

    public void addNumbers() {
        int a = 10;
        String b = "20";
        int c = a + Integer.parseInt(b);
    }

    public void calculateSum() {
        int x = 0;
        int result = x + 10;
    }

    public int multiply(int a, int b) {
        if (a > 0)
        if (a > 0)
            return a * b;
        else
            return 0;
    }
}
